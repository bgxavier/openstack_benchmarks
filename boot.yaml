---
  NovaServers.boot_server:
    -
      args:
        profile:
        flavor:
            name: "m1.tiny"
        image:
            name: "larsks/thttpd"
      runner:
        type: "constant"
        times: 60
        concurrency: 1
      context:
        users:
          tenants: 1
          users_per_tenant: 1
